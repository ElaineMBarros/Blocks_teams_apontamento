# Server Configuration
PORT=3000

# Teams App Configuration
TEAMS_APP_ID=your-teams-app-id-here
HOSTNAME=your-hostname.ngrok.io

# For local development, you can use ngrok or similar tunneling service
# Example: ngrok http 3000
# Then update HOSTNAME with the ngrok URL
